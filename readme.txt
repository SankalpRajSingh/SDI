This readme is used for git tutorial
1.How to dowload git bash for windows
2.git command
         git config --global user.name "sankalp.s"
         git config --global user.email "rajsankalp805@gmail.com"
	 git status
	 git add
	 git loig

how to add git :
         mkdir testgit              # creates a directory
	 cd tstgit                  # enters the directory
	 git init                   # initialization of git
	 vim readme.txt             # creating readme file
	 git status                 # show the status of git as red
	 git add readme.txt         # 
         git status                              # status changes to green after adding add readme.txt step 
	 git commit -o "Added Readme file"       # to commit the git commits
	 git log                                 # to show the commit logs 
